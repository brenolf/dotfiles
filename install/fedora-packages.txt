# dnf packages for Breno's PERSONAL (Fedora/Nobara) machine.
# Consumed by install/install.sh (install_personal).
#
# Handled separately in the script (not via dnf):
#   - starship (official install script)
#   - MesloLGS Nerd Font (downloaded from the nerd-fonts release)
tmux
alacritty
btop
eza
tldr
